import 'package:flutter/material.dart';

/// Subjects resources or support material files
class SubjectResources extends StatelessWidget {
  ///
  const SubjectResources({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
